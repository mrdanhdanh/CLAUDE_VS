# PRD: Harness 2.1 — P0-3 Planning Structured Output + Router (Lesson 07)

> **Version:** 2.1-P0-3 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **Nguồn:** `AI-Agents-for-Beginners-Distilled.md` Lesson 07 Planning Design + `docs/harness-2.1-upgrade.md` §5.3
> **Baseline:** Harness 2.0 — `plan.md` markdown tự do, `Implement` parse tay → dễ lỗi, không route được
> **Pipeline:** Harness 8 phase — Explore(quick) → Clarify(1 câu) → PRD mini → Design mini → Plan(3 todos) → Implement → Polish → Verify

## 1. Vision
- **One-liner:** Bắt `Plan` trả JSON theo schema (Pydantic/Zod) để downstream parse được + router điều phối subtask cho agent chuyên môn, như Lesson 07.
- **Problem:** `plan.md` hiện là markdown tự do, `Implement` phải đọc tay → dễ miss, không route tự động cho multi-agent.
- **Target User:** Plan agent + Implement/Verify + harness router.

## 2. User Stories
| ID | As a ... | I want ... | So that ... | Priority |
|----|----------|------------|-------------|----------|
| US-01 | Plan agent | `plan.md` có frontmatter `plan: {main_task, subtasks[], is_greeting}` valid | Implement parse không lỗi | P0 |
| US-02 | Implement | `plan-validate.mjs --file plan.md` pass/fail rõ ràng | Biết plan có structured không | P0 |
| US-03 | Harness | Router đọc subtasks → delegate đúng agent (`assigned_agent: implement` → Implement) | Multi-agent thực sự | P0 |
| US-04 | Dev | Plan cũ không frontmatter vẫn pass (warning) | Backward compat | P0 |

## 3. Scope

### In Scope (P0 — phải có)
- [ ] `.github/harness/scripts/plan-validate.mjs` — **MỚI**, Node 18+, 0 deps: parse frontmatter, validate schema, `--route` in dispatcher map, exit 0/1/2
- [ ] Sửa `.github/skills/claude-harness/templates/plan-template.md` — thêm frontmatter mẫu
- [ ] Sửa `.github/agents/plan.agent.md` — thêm instruction trả structured output
- [ ] Tests: valid plan → pass, missing subtasks → fail, legacy markdown → pass+warning, wrong agent → fail

### Nice to Have (P1)
- [ ] Tích hợp `harness-manager.mjs` lệnh `validate-plan`
- [ ] Event-driven iterative planning

### Non-Goals
- Multi-agent group chat (P1 khác), LLM router (chỉ rule-based v1)

## 4. Success Metrics
- **Metric 1:** `plan-validate.mjs` với plan P0-1/P0-2 (legacy) → pass + warning (không fail)
- **Metric 2:** plan mới có frontmatter valid → pass, router in đúng agent
- **Metric 3:** plan sai `assigned_agent: foo` → fail với lỗi rõ ràng
- **Metric 4:** `get_errors` pass, không vỡ pipeline cũ

## 5. Edge Cases & Constraints
- **Legacy plan:** không có frontmatter → pass + warning `legacy markdown`
- **Empty subtasks:** fail `subtasks must have 1-10 items`
- **Constraint:** Node 18+, 0 deps, minimal YAML parser tự viết (không js-yaml)
- **Persistence · F5 · Scope:** Không liên quan `www/`

## 6. Assumptions
- Frontmatter YAML subset đủ (không cần full YAML)
- AgentEnum: `explore|plan|design|implement|polish|verify|yunie`
- Router v1 chỉ in map, chưa auto-delegate (để P1)

## 7. Open Questions
- [x] JSON hay YAML frontmatter? → **YAML subset** (quen với plan-template, dễ đọc)
- [x] Validate ở đâu? → **CLI `plan-validate.mjs` + instruction trong `plan.agent.md`**

---
*Generated by Claude Harness v2 — PRD Phase (P0-3 Planning)*
