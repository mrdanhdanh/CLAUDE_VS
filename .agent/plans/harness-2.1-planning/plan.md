# Plan: Harness 2.1 — P0-3 Planning Structured Output + Router

> **Version:** 2.1-P0-3 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Design:** `design.md`

## 1. Architecture

**Stack:** Node 18+ ESM, 0 deps, minimal YAML parser tự viết (không js-yaml).

```
.github/
  harness/scripts/plan-validate.mjs   ← MỚI: parse + validate + route
  skills/claude-harness/templates/plan-template.md  ← SỬA: thêm frontmatter mẫu
  agents/plan.agent.md                ← SỬA: thêm structured output instruction
```

**Data flow:** `plan.md` → `parseFrontmatter` → `validatePlan` → `routePlan` → dispatcher map

## 2. File Changes

| File | Action | Mô tả |
|------|--------|-------|
| `.github/harness/scripts/plan-validate.mjs` | **CREATE** | ESM, exports + CLI |
| `.github/skills/claude-harness/templates/plan-template.md` | **EDIT** | Thêm frontmatter mẫu |
| `.github/agents/plan.agent.md` | **EDIT** | Thêm structured output section |
| `.agent/plans/harness-2.1-planning/*` | **CREATE** | prd/design/plan |

**Không đụng:** pipeline cũ, `registry.json`, `www/`

## 3. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| YAML parser tự viết lỗi edge | Medium | Medium | Chỉ support subset (plan/main_task/subtasks), test với plan thật |
| Plan cũ fail | Low | High | Legacy → pass + warning, không fail |
| Template sửa vỡ | Low | Low | Chỉ thêm frontmatter comment, giữ body cũ |

## 4. Todos

| # | Todo | Owner | Est |
|---|------|-------|-----|
| 1 | Tạo `plan-validate.mjs` (parse + validate + route) | Implement | 30m |
| 2 | Sửa `plan-template.md` thêm frontmatter mẫu | Implement | 10m |
| 3 | Sửa `plan.agent.md` thêm structured instruction | Implement | 10m |
| 4 | Verify: legacy + valid + invalid + `get_errors` | Verify | 15m |

**Total:** ~65m

## 5. Verification Plan

- `plan-validate.mjs --file .agent/plans/harness-2.1-rag-loop/plan.md` → pass + warning (legacy)
- Tạo plan test valid → pass, `--route` in đúng map
- Plan sai agent → fail rõ ràng
- `get_errors` pass

## 6. Rollback

- Xóa `plan-validate.mjs`, revert template/agent về HEAD.

---
*Generated by Claude Harness v2 — Plan Phase (P0-3 Planning)*
