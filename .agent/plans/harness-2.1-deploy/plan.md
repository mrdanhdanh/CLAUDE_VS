# Plan: Harness 2.1 — P1-6 Deploy Scalable

> **Version:** 2.1-P1-6 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Design:** `design.md`

## 1. Architecture

**Stack:** Node 18+ ESM, 0 deps, browser+Node.

```
www/library/
  router.mjs        ← MỚI: route + cache
  app.js            ← EDIT: dùng router
  mcp-server.mjs    ← EDIT: dùng router + routed/cached
.github/harness/scripts/
  deploy-check.mjs  ← MỚI: lifecycle
```

## 2. File Changes

| File | Action | Mô tả |
|------|--------|-------|
| `www/library/router.mjs` | **CREATE** | routeQuery + cache Map |
| `www/library/app.js` | **EDIT** | doSearch + searchIterative qua router |
| `www/library/mcp-server.mjs` | **EDIT** | search handlers qua router |
| `.github/harness/scripts/deploy-check.mjs` | **CREATE** | version + eval + audit |
| `.agent/plans/harness-2.1-deploy/*` | **CREATE** | prd/design/plan |

## 3. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Router sai (simple nhưng cần deep) | Medium | Low | Heuristic conservative: nghi ngờ → deep; test với queries thật |
| Cache stale | Low | Low | TTL 5m ngắn, key gồm top_k + enabled |
| MCP cache giữa requests | Low | Low | In-memory process, max 50 |

## 4. Todos

| # | Todo | Owner | Est |
|---|------|-------|-----|
| 1 | Tạo `router.mjs` | Implement | 25m |
| 2 | Sửa `app.js` + `mcp-server.mjs` dùng router | Implement | 25m |
| 3 | Tạo `deploy-check.mjs` + Verify | Verify | 20m |

**Total:** ~70m

## 5. Verification Plan

- routeQuery simple/complex đúng
- cache hit lần 2
- deploy-check pass
- `get_errors` pass, MCP backward compat

## 6. Rollback

- Xóa router/deploy-check, revert app/mcp về HEAD.

---
*Generated by Claude Harness v2 — Plan Phase (P1-6 Deploy)*
