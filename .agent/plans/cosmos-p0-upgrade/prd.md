# PRD mini — cosmos-p0-upgrade

Cosmic-Quantum: Macro www/cosmos là vũ trụ con trong www/ · Micro superposition 3 hướng → collapse 1 (giữ index.html canonical) · Entanglement www/cosmos.html ↔ www/cosmos/index.html ↔ www/cosmos/slides.html ↔ www/cosmos/scale.html ↔ www/cosmos/scale.json ↔ .github/skills/cosmic-quantum/SKILL.md ↔ .github/harness/scripts/cosmic-scale.mjs

## Vision
Gộp duplicate cosmos, nối Black Hole lab với S thật, thống nhất System Map 15, lưu lịch sử S.

## Scope GIỮ
- 1 canonical `www/cosmos/index.html`, `www/cosmos.html` redirect nhẹ
- Black Hole lab khởi tạo từ `scale.json` thật
- Skill ghi đúng 15 map
- `scale.json` có `history[]` + dashboard vẽ trend

## CẮT (YAGNI)
- Không tách CSS/JS riêng, không thêm lab mới, không sinh slides tự động, không entanglement graph (để P1/P2)

## Metrics
- Không còn 2 file 114KB duplicate; `S` lab = `S` scale.json lúc load; `scale.json` có history; verify pass

Persistence: repo path `www/cosmos/scale.json` · F5: giữ (file) · Scope: global
