# PRD: Harness 2.1 — P1-5 Memory Tiers (Lesson 13)

> **Version:** 2.1-P1-5 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **Nguồn:** `AI-Agents-for-Beginners-Distilled.md` Lesson 13 Agent Memory + `docs/harness-2.1-upgrade.md` §5.6c
> **Baseline:** `docs/knowleged.md` (long-term) + `.agent/bugs/` (episodic), chưa phân tầng working/short/long/persona
> **Pipeline:** Harness 8 phase

## 1. Vision
- **One-liner:** Biến stateless thành stateful — working/short/long/persona/episodic — với CLI `memory.mjs` + self-improving pattern.
- **Problem:** Chỉ có long-term + episodic rời rạc, không có working memory cho task hiện tại, không có persona consistent.
- **Target User:** Mọi agent (Explore → Verify), YUNIE.

## 2. User Stories
| ID | As a ... | I want ... | So that ... | Priority |
|----|----------|------------|-------------|----------|
| US-01 | Agent | `memory.mjs remember --tier working --key "task" --value "..."` → lưu scratch cho task hiện tại | Không mất context giữa turns | P0 |
| US-02 | Agent | `memory.mjs recall --tier long --query "rainbow"` → BM25 trên knowleged KNs | Retrieve đúng bài học | P0 |
| US-03 | YUNIE | `memory.mjs persona --get` → role consistent (barista công nghệ) | Persona không drift | P0 |
| US-04 | Dev | Self-improving: observe → extract → store → augment (như RAG) | Memory tự lớn | P1 |

## 3. Scope

### In Scope (P0 — phải có)
- [ ] `.github/harness/scripts/memory.mjs` — **MỚI**: `remember`, `recall`, `forget`, `persona`, `episodic`, tiers `working|short|long|persona|episodic`
- [ ] `.agent/memory/` — tiers as JSONL: `working.jsonl` (gitignore, ephemeral), `short.jsonl` (gitignore), `long.jsonl` (commit? không — long = knowleged.md, chỉ index), `persona.json` (commit), `episodic.jsonl` (commit, từ experience.jsonl nếu có)
- [ ] `recall --tier long` dùng BM25-lite trên `docs/knowleged.md` (reuse `auto-learn.mjs` suggest logic? đơn giản: grep + score)
- [ ] Tests: remember/recall/forget/persona, redact secret

### Nice to Have (P1)
- [ ] Mem0/Cognee adapter, Azure AI Search backend
- [ ] Structured RAG (entity extract)

### Non-Goals
- Vector DB (P2), graph memory (P2)

## 4. Success Metrics
- **Metric 1:** `remember working` → `recall working` đúng value
- **Metric 2:** `recall long --query "rainbow"` → KN-003/KN-004
- **Metric 3:** `persona --get` → YUNIE persona, `--set` update được
- **Metric 4:** `get_errors` pass, secret redact

## 5. Edge Cases & Constraints
- **Secret:** remember tự redact `sk-`/`token`, recall không trả secret
- **Working/short:** gitignore (ephemeral), long/persona/episodic commit
- **Constraint:** Node 18+, 0 deps
- **Persistence:** working F5 mất (ephemeral file local), long/persona F5 giữ (commit)

## 6. Assumptions
- File-based JSONL đủ cho v1, không cần DB
- Long-term recall = grep knowleged (đơn giản, đủ tốt)

## 7. Open Questions
- [x] Tiers nào commit? → **persona + episodic commit, working/short gitignore**
- [x] Long recall bằng gì? → **Grep knowleged + score (reuse pattern auto-learn)**

---
*Generated by Claude Harness v2 — PRD Phase (P1-5 Memory)*
