# Design: Harness 2.1 — P1-5 Memory Tiers

> **Version:** 2.1-P1-5 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Nguồn:** Lesson 13 Memory

## 1. Design System
Không UI.

## 2. Wireframe

### Tiers
```
working.jsonl   (ephemeral, gitignore) — scratch paper task hiện tại
short.jsonl     (ephemeral, gitignore) — 1 session/conversation
long            (docs/knowleged.md, commit) — recall via grep, không duplicate
persona.json    (commit) — {role, traits, language}
episodic.jsonl  (commit) — sequence success/failure (link experience.jsonl nếu có)
```

### CLI
```bash
node memory.mjs remember --tier working --key "task" --value "P1-5 memory"
node memory.mjs recall --tier working --key "task"
node memory.mjs recall --tier long --query "rainbow"
node memory.mjs forget --tier working --key "task"
node memory.mjs persona --get
node memory.mjs persona --set --role "barista" --traits "genz,warm" --language "vi"
node memory.mjs episodic --log --task "P1-5" --outcome "pass" --note "..."
```

### Recall Long (BM25-lite đơn giản)
- Parse `docs/knowleged.md` bảng KN (ID + Bug + Bài học), score = count query tokens in row, trả top 3.

## 3. Component Inventory

| Component | Action | Notes |
|-----------|--------|-------|
| `.github/harness/scripts/memory.mjs` | **CREATE** | ESM 0 deps |
| `.agent/memory/` | **CREATE** | 5 tiers |
| `.gitignore` | **EDIT** | Thêm working/short gitignore (giữ long/persona/episodic) |
| `.agent/plans/harness-2.1-memory/*` | **CREATE** | prd/design/plan |

## 4. Self-Improving Pattern (P1)
Observe → Extract → Store → Augment (như RAG) — v1 chỉ manual via CLI, auto để P1.

---
*Generated by Claude Harness v2 — Design Phase (P1-5 Memory)*
