# PRD (mini) — Cosmos Slides Refresh

- **Ngày:** 2026-09-12 · **Actor:** YUNIE · **Nguồn:** user yêu cầu "update lại nội dung trang slide" (đang xem `www/cosmos/slides.html#slide-15`).
- **Vấn đề:** `slides.html` (clip 15 slide) lệch với trạng thái thật sau commit `125ffc5`: (1) aria-label 7 slide đầu còn "trên 12" dù đã 15 slide; (2) slide 11 eyebrow còn số cũ "08 · Lab"; (3) placeholder thời lượng "01:12" (12 slide cũ) thay vì "01:30" (15×6s); (4) slide 10 ghi D=9 trong khi scale.json vừa đo **D=8**; (5) slide 15 "Tương lai" còn card đã-ship + "Multiverse picker → backlog", chưa có **6 đề tài mới** đã refresh ở `index.html#future-title`.
- **Giải pháp:** Đồng bộ slides với trạng thái thật 2026-09-12: sửa numbering/time/số liệu; viết lại slide 15 = dòng "Đã ship & gỡ khỏi backlog" (Lượng tử Decision · Lab 11 thí nghiệm · Heat Death · Entanglement Graph) + list 6 đề tài mới (Cosmic Web v2, Hawking Radiation, CMB Anisotropy, Gravitational Waves, QEC Lab #12, Wormhole) đồng bộ wording index.html.
- **Non-goals:** Không sửa `index.html`/`scale.html`; không đổi cơ chế điều khiển clip (play/timeline/orientation); không thêm slide (giữ 15); không fetch động (clip phải chạy offline ổn định).
- **Persistence · F5 · Scope:** Trang tĩnh 100%, không ghi dữ liệu người dùng — Persistence: none · F5: n/a · Scope: n/a. Số liệu slide 10 là snapshot có nhãn nguồn ("số thật từ scale.json" — regenerate bằng `npm run cosmos:refresh`).
- **Cosmic-Quantum:** Macro clip = "phim tài liệu" của vũ trụ `www/cosmos` · Micro đo bằng Playwright (count 15, time 01:30, nội dung slide 15) · Entanglement: `slides.html` ↔ `index.html#future-title` ↔ `scale.json`.
- **Who did you think with? (Dissent — KN-018):** Framing đối lập: *"slide là snapshot có chủ đích — sync tay mỗi lần sẽ lệch lại ngay; sao không render slide 15 động từ index.html?"*. Xử lý: giữ tĩnh (clip chạy offline, không phụ thuộc fetch — KN-030 đã vấp với fetch sai path), nhưng thêm regression test `cosmos-slides.spec.ts` để lần lệch sau bị test bắt thay vì để user phát hiện.
