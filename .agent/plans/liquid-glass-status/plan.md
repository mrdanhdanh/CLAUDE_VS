# Plan: Liquid Glass Status — Rainbow Hover

## 1. Context
- PRD: `.agent/plans/liquid-glass-status/prd.md`
- Design: `.agent/plans/liquid-glass-status/design.md`
- Skill: `glass-rainbow-effects` (liquid glass + rainbow border)
- Target: `www/styles.css` (chính), `www/index.html` (class hooks nếu cần), `www/app.js` (không đổi logic)
- Constraint: chỉ CSS, không lib, giữ layout, a11y, responsive 375/768/1280

## 2. Work Breakdown

| # | Task | Files | Est |
|---|------|-------|-----|
| T1 | Tokens + body mesh + @property + fallback | `www/styles.css` :root, body, @supports | 15m |
| T2 | Header glass (navigation layer) | `www/styles.css` .header | 10m |
| T3 | Glass cho mọi card chính (hero, mini, stat, card, yunie, reg, page-link, howto, modal) | `www/styles.css` | 25m |
| T4 | Rainbow border động hover (pseudo ::before + conic-gradient + mask + rotate) | `www/styles.css` | 20m |
| T5 | Polish: reduced-motion, reduced-transparency, focus-within, contrast, responsive check | `www/styles.css` | 15m |
| T6 | Verify: get_errors + visual 375/768/1280 + keyboard | — | 10m |

## 3. Todos (manage_todo_list)

- T1: Thêm tokens glass/rainbow + body mesh
- T2: Header liquid glass
- T3: Glass cho cards/sections
- T4: Rainbow hover xoay
- T5: A11y polish (motion/transparency/focus)
- T6: Verify build + visual

## 4. Files to Change
- `www/styles.css` — 90% công việc (tokens, body, header, cards, rainbow, a11y)
- `www/index.html` — chỉ nếu cần thêm class `glass-hover-rainbow` (ưu tiên CSS-only, không đổi HTML nếu không cần)
- Không đổi `www/app.js`, `www/status.json`

## 5. Risks & Mitigations
- **Backdrop-filter performance** → blur 16px vừa phải, không blur quá lớn; fallback solid
- **Mask composite Safari** → dùng cả `-webkit-mask-composite: xor` + `mask-composite: exclude`
- **Rainbow che content** → `pointer-events:none`, `inset:0`, `padding: var(--border-w)`
- **Contrast trên glass** → fill 0.68-0.78, test text #0f172a ≥4.5:1
- **Nhiều glass gần nhau double-filter** → cards cách nhau 12-16px, không chồng, ok

## 6. Verification
- [ ] `get_errors` 0
- [ ] Visual: header kính mờ, mọi card kính mờ, hover viền cầu vồng xoay mượt
- [ ] Responsive 375/768/1280 không vỡ
- [ ] Keyboard Tab → focus-within hiện rainbow + outline
- [ ] `prefers-reduced-motion: reduce` tắt xoay
- [ ] Fallback khi `backdrop-filter` không hỗ trợ → solid white

---
*Generated by Harness v2 — Plan Phase — liquid-glass-status*
