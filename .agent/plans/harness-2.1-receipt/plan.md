# Plan: Harness 2.1 — P0-4 Receipt Ed25519 + JCS

> **Version:** 2.1-P0-4 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Design:** `design.md`

## 1. Architecture

**Stack:** Node 18+ `node:crypto` Ed25519, 0 deps.

```
.agent/
  scripts/audit.mjs   ← EDIT: JCS + sign/verify + keygen
  audit.key           ← CREATE runtime (gitignore)
  audit.jsonl         ← events thêm sig/pub (gitignore)
```

## 2. File Changes

| File | Action | Mô tả |
|------|--------|-------|
| `.agent/scripts/audit.mjs` | **EDIT** | Thêm `canonicalize`, `loadOrCreateKey`, `signEvent`, `verifySig`, `cmdKeygen`, `cmdPubkey`, sửa `cmdLog` + `cmdVerify` |
| `.gitignore` | **EDIT** | Thêm `.agent/audit.key` |
| `.agent/plans/harness-2.1-receipt/*` | **CREATE** | prd/design/plan |

## 3. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Node không hỗ trợ ed25519 | Low | High | Check `crypto.sign` + fallback: nếu fail thì log không sig + warning (không block) |
| Key mất → events mới không verify cũ | Low | Medium | Mỗi event mang pub riêng, verify độc lập |
| JCS khác implement → verify fail | Low | Medium | Tự viết canonicalize đơn giản, test round-trip |

## 4. Todos

| # | Todo | Owner | Est |
|---|------|-------|-----|
| 1 | Sửa `audit.mjs` thêm JCS + Ed25519 + keygen | Implement | 40m |
| 2 | Thêm `.agent/audit.key` vào `.gitignore` | Implement | 5m |
| 3 | Verify: keygen + log + verify + tamper test | Verify | 20m |

**Total:** ~65m

## 5. Verification Plan

- `audit.mjs keygen` → tạo key, `pubkey` in public
- `audit.mjs log --tool test --target "p0-4" --decision permitted` → event có sig/pub
- `audit.mjs verify` → chain OK + sig OK
- Sửa 1 byte → verify fail
- `get_errors` + `policy-check --check` pass

## 6. Rollback

- Revert `audit.mjs` về HEAD, xóa `.agent/audit.key`. Events có sig vẫn verify hash-chain cũ được (bỏ qua sig).

---
*Generated by Claude Harness v2 — Plan Phase (P0-4 Receipt)*
