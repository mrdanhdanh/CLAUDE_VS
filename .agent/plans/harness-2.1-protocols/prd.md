# PRD: Harness 2.1 — P1-3 Protocols MCP/A2A Hardening (Lesson 11)

> **Version:** 2.1-P1-3 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **Nguồn:** `AI-Agents-for-Beginners-Distilled.md` Lesson 11 Agentic Protocols + `docs/harness-2.1-upgrade.md` §5.6a
> **Baseline:** `.agent/mcp/catalog.json` (2 vendors demo), `mcp-check.mjs`, `agents.yaml` (3 built-in), library MCP 5 tools chưa pin version
> **Pipeline:** Harness 8 phase

## 1. Vision
- **One-liner:** Chuẩn hóa protocols: MCP pin version + scoped identity + output validation; A2A Agent Card validate + registry.
- **Problem:** MCP catalog chỉ demo, chưa có library vendor; library MCP chưa pin version/auth; A2A chưa có card validation.
- **Target User:** Platform (MCP grants), YUNIE (agent registry), auditor.

## 2. User Stories
| ID | As a ... | I want ... | So that ... | Priority |
|----|----------|------------|-------------|----------|
| US-01 | Platform | `mcp-check.mjs` validate library vendor (version pin, scoped tools) | Governed MCP thực sự | P0 |
| US-02 | Dev | `www/library/mcp-server.mjs` expose `version: 1.1.0` + validate output (no secret leak) | MCP production-ready | P0 |
| US-03 | YUNIE | `agent-card.mjs` validate A2A card (name, skills, endpoint, version) cho `agents.yaml` | A2A interop | P0 |
| US-04 | Auditor | Unknown tool → refused (fail-closed) đã có, giữ | Không regression | P0 |

## 3. Scope

### In Scope (P0 — phải có)
- [ ] Thêm `library` vendor vào `.agent/mcp/catalog.json` (version `1.1.0`, tools 5, auth `none/local`, scope `read-only`)
- [ ] Sửa `www/library/mcp-server.mjs`: `serverInfo.version 1.1.0`, validate output (redact nếu text chứa `sk-`/`token`), thêm `protocolVersion` pin
- [ ] `.github/harness/scripts/agent-card.mjs` — **MỚI**: validate A2A card (required fields, endpoint https hoặc allowlist, version semver), CLI `--file agents.yaml` / `--card <json>`
- [ ] Sửa `.agent/scripts/mcp-check.mjs` nếu cần để check library vendor (đọc catalog)
- [ ] Tests: catalog valid, MCP version, output no leak, card valid/invalid

### Nice to Have (P1)
- [ ] NLWeb embeddings endpoint, grants per-agent cho library
- [ ] MCP auth scoped identity

### Non-Goals
- Remote A2A executor, vector DB (P2)

## 4. Success Metrics
- **Metric 1:** `mcp-check.mjs --tool search_library --agent general` → permitted với library vendor
- **Metric 2:** `tools/list` trả `version 1.1.0`, output không chứa secret mẫu
- **Metric 3:** `agent-card.mjs --file .agent/agents.yaml` → 3 cards valid
- **Metric 4:** `get_errors` pass, unknown tool vẫn refused

## 5. Edge Cases & Constraints
- **Catalog thiếu library:** fail rõ ràng, hướng dẫn thêm vendor
- **Output leak:** nếu chunk text chứa `sk-...` → redact thành `***` (như audit)
- **Constraint:** Node 18+, 0 deps
- **Persistence:** catalog commit, version bump 1.0.0 → 1.1.0

## 6. Assumptions
- Library MCP là read-only nên auth `none/local` đủ cho v1
- A2A v1 chỉ validate card, chưa execute remote

## 7. Open Questions
- [x] Version pin ở đâu? → **`serverInfo.version` + catalog `version`**
- [x] Secret trong output? → **Redact như audit**

---
*Generated by Claude Harness v2 — PRD Phase (P1-3 Protocols)*
