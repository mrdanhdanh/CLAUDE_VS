# Plan: Harness 2.2 — P2-1 MAF Workflows

> **Version:** 2.2-P2-1 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Design:** `design.md`

## 1. Architecture

**Stack:** Node 18+ ESM, 0 deps. Reuse `handoff.mjs`, `trace.mjs`, `audit.mjs` (best-effort, không block).

```
.github/harness/scripts/workflow.mjs ← MỚI
.agent/runs/<runId>.json ← checkpoints (gitignore)
```

## 2. File Changes

| File | Action | Mô tả |
|------|--------|-------|
| `.github/harness/scripts/workflow.mjs` | **CREATE** | Definitions + run/resume/status/list + when evaluator |
| `.gitignore` | **EDIT** | Thêm `.agent/runs/` |
| `.agent/plans/harness-2.2-maf/*` | **CREATE** | prd/design/plan |

**Không đụng:** pipeline cũ, agents, prompts.

## 3. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| `when` evaluator lỗi | Medium | Medium | Chỉ support subset đơn giản, test với ui_changed true/false |
| Tích hợp handoff/trace/audit fail | Low | Low | Best-effort try/catch, không block run |
| Checkpoint corrupt | Low | Low | JSON write atomic (write tmp + rename) |

## 4. Todos

| # | Todo | Owner | Est |
|---|------|-------|-----|
| 1 | Tạo `workflow.mjs` (definitions + engine) | Implement | 40m |
| 2 | Thêm `.agent/runs/` gitignore | Implement | 5m |
| 3 | Verify: run/branch/approval/resume | Verify | 20m |

**Total:** ~65m

## 5. Verification Plan

- `--list` → 2 workflows
- run ui_changed=false → skip polish
- run không auto-approve → paused, resume --approve → done
- resume idempotent (done → already done)
- `get_errors` pass

## 6. Rollback

- Xóa workflow.mjs + revert gitignore.

---
*Generated by Claude Harness v2 — Plan Phase (P2-1 MAF Workflows)*
