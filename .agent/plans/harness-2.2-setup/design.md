# Design: Harness 2.2 — P2-4 Setup Doctor

> **Version:** 2.2-P2-4 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **PRD:** `prd.md` · **Nguồn:** Lesson 00 Setup

## 1. Design System
Không UI (CLI output).

## 2. Wireframe

### Checks
```
1. node: process.version >= 18
2. files: 11 scripts + registry.json + policy.json + catalog.json + knowleged.md
3. mcp: catalog.json + grants.json parse OK, library vendor present
4. env: FOUNDRY_LOCAL_ENDPOINT/FOUNDRY_LOCAL_MODEL/AZURE_* → set|missing (no values)
5. ports: 5251 (dotnet, KN-008) + 12434 (foundry local) → warn nếu LISTENING (lsof/ss, best-effort)
6. git: .env không tracked (git ls-files), audit.key không tracked
```

### Output
```
Setup doctor: ✅ PASS (5/5) | ⚠️ WARN ports | ❌ FAIL files
  ✅ node: v22.15.0 (>=18)
  ✅ files: 15/15 present
  ✅ mcp: catalog 3 vendors, grants OK
  ○ env: FOUNDRY_LOCAL_ENDPOINT=missing (warn, optional)
  ⚠️ ports: 5251 LISTENING (warn — stop dotnet run before build, KN-008)
```

## 3. Component Inventory

| Component | Action | Notes |
|-----------|--------|-------|
| `.github/harness/scripts/setup-doctor.mjs` | **CREATE** | ESM 0 deps |
| `.agent/plans/harness-2.2-setup/*` | **CREATE** | prd/design/plan |

## 4. CLI

```bash
node .github/harness/scripts/setup-doctor.mjs
node .github/harness/scripts/setup-doctor.mjs --json
```

---
*Generated by Claude Harness v2 — Design Phase (P2-4 Setup)*
