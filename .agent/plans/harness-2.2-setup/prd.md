# PRD: Harness 2.2 — P2-4 Course Setup Doctor (Lesson 00)

> **Version:** 2.2-P2-4 · **Ngày:** 2026-09-04 · **Tác giả:** YUNIE
> **Nguồn:** `AI-Agents-for-Beginners-Distilled.md` Lesson 00 Setup + KN-008/KN-009 (env/config)
> **Baseline:** Chưa có setup doctor; dev tự mò Node version, env, ports, IndexedDB
> **Pipeline:** Harness 8 phase

## 1. Vision
- **One-liner:** `setup-doctor.mjs` kiểm tra môi trường Harness 2.x — Node, files, MCP, env, ports — như Lesson 00 checklist, không đụng secret.
- **Problem:** Onboarding mới / máy mới hay thiếu Node 18, thiếu file, env sai, port kẹt (KN-008), hardcode URL (KN-009).
- **Target User:** Dev mới, CI, YUNIE health check.

## 2. User Stories
| ID | As a ... | I want ... | So that ... | Priority |
|----|----------|------------|-------------|----------|
| US-01 | Dev | `setup-doctor.mjs` → PASS/FAIL per check (node, files, mcp, env, ports) | Biết thiếu gì trong 10s | P0 |
| US-02 | CI | `--json` + exit 1 khi fail → block | Gate onboarding | P0 |
| US-03 | Dev | Không in secret (chỉ `set/missing`) | An toàn như audit redact | P0 |

## 3. Scope

### In Scope (P0 — phải có)
- [ ] `.github/harness/scripts/setup-doctor.mjs` — **MỚI**: checks node>=18, required files (11 scripts + registry + policy), MCP catalog/grants valid JSON, env presence (no values), ports 5251/12434 (warn nếu LISTENING), `.env` không commit
- [ ] Tests: pass trên repo hiện tại, fail khi thiếu file (mock), secret không in
- [ ] `.agent/plans/harness-2.2-setup/*` prd/design/plan

### Nice to Have (P1)
- [ ] Auto-fix (`--fix` tạo file thiếu), az login check
- [ ] Python/requirements check (Lesson 00 gốc là Python)

### Non-Goals
- Cài Node/deps tự động (chỉ diagnose v1), Foundry/Azure check sâu

## 4. Success Metrics
- **Metric 1:** `setup-doctor.mjs` trên repo → PASS (hoặc chỉ warn ports)
- **Metric 2:** Xóa 1 file required (tmp) → FAIL rõ ràng
- **Metric 3:** Không có secret value nào trong output
- **Metric 4:** `get_errors` pass

## 5. Edge Cases & Constraints
- **Ports:** chỉ warn (không fail) vì dev có thể đang chạy app
- **Env:** chỉ check presence (`set/missing`), không in value
- **Constraint:** Node 18+, 0 deps
- **Lesson 00 gốc:** Python + az login — v1 chỉ Node + files + MCP (Harness là Node)

## 6. Assumptions
- Harness 2.x là Node-first (không cần Python cho v1)
- `.env` không bắt buộc (chỉ warn nếu thiếu file mẫu)

## 7. Open Questions
- [x] Check gì? → **Node, files, MCP JSON, env presence, ports warn**
- [x] Có auto-fix? → **Không v1 — chỉ diagnose**

---
*Generated by Claude Harness v2 — PRD Phase (P2-4 Setup)*
