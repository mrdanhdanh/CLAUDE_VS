# Plan: WEB 011 Part 3 — GRAPHICS LAB

> Phased plan cho Graphics Lab. Kế thừa Core Runtime, không sửa core.

## 1. Architecture
- **Stack:** Vanilla JS + SVG + WebGL + WebGPU (detection)
- **Pattern:** Mỗi module ES module lazy-load, mount vào window body
- **State:** localStorage per-module
- **No deps:** Không thêm lib

## 2. File Changes

| File | Action | Description |
|------|--------|-------------|
| `www/web-universe/js/modules/svg-lab/index.js` | Create | SVG shapes + props + drag + export |
| `www/web-universe/js/modules/webgl-lab/index.js` | Create | WebGL triangle/texture/cube + controls |
| `www/web-universe/js/modules/webgpu/index.js` | Create | WebGPU detection + adapter/device info |
| `www/web-universe/css/modules.css` | Update | Append styles cho 3 modules |
| `www/web-universe/js/app.js` | Update | CATALOG: 3 stubs → real (svg-lab, webgl-lab, webgpu) |

## 3. Todos (5 phases)

### Phase 1 — SVG Lab
- [ ] `js/modules/svg-lab/index.js` — palette + canvas + props + drag + export + persist

### Phase 2 — WebGL Lab
- [ ] `js/modules/webgl-lab/index.js` — WebGL context + triangle/cube + controls + capability

### Phase 3 — WebGPU
- [ ] `js/modules/webgpu/index.js` — detection + adapter/device + demo or NOT SUPPORTED

### Phase 4 — Styles + Catalog
- [ ] `css/modules.css` — append styles cho svg/webgl/webgpu
- [ ] `js/app.js` — update CATALOG 3 entries

### Phase 5 — Verify
- [ ] `get_errors` 0, `node --check` all, manual test, lazy-load, F5 persist, STATUS

## 4. Risks

| Risk | Mitigation |
|------|------------|
| WebGL not supported | Check `getContext` null → show fallback, not crash |
| WebGPU not supported | Check `navigator.gpu` → NOT SUPPORTED, no fake |
| SVG many shapes slow | Limit 100 shapes, debounce render |
| WebGL context lost | Handle `webglcontextlost`, show message |
| CSS conflict | Prefix `.svg-`, `.webgl-`, `.webgpu-` |

## 5. Verification Checklist

- [ ] `get_errors` → 0
- [ ] SVG: add circle → drag → props update → export .svg
- [ ] WebGL: triangle/cube render, FPS, capability info
- [ ] WebGPU: detection correct, NOT SUPPORTED if not available
- [ ] F5 persist, Network lazy-load, responsive, a11y

---
*Generated by YUNIE — Harness v2 Plan Phase — Part 3 Graphics Lab*
